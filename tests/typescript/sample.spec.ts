import { describe, it, expect } from 'vitest';

// Simple placeholder test to verify TS test wiring
// Add real unit tests as functionality grows

describe('math sanity', () => {
  it('adds numbers', () => {
    expect(2 + 2).toBe(4);
  });
});
